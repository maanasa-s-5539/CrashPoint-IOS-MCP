# Do NOT add auth tokens here — use ~/.npmrc or the NPM_TOKEN environment variable.
# Example (in ~/.npmrc only, never commit tokens):
#   //npm.pkg.github.com/:_authToken=${NPM_TOKEN}
@maanasa-s-5539:registry=https://npm.pkg.github.com/
